<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ListView
        android:id="@+id/order_list_datas"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toLeftOf="@+id/order_list_ob_index"
        android:layout_toStartOf="@+id/order_list_ob_index"
        android:scrollbars="none" />

    <com.yalin.indexablelistview.IndexBar
        android:id="@+id/order_list_ob_index"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:padding="8dp"
        app:backgroundColor="@android:color/transparent"
        app:textColor="#7294fb"
        app:textSize="12sp" />

    <TextView
        android:id="@+id/order_list_tv_select"
        android:layout_width="60dp"
        android:layout_height="50dp"
        android:layout_marginEnd="20dp"
        android:layout_marginRight="20dp"
        android:layout_toLeftOf="@+id/order_list_ob_index"
        android:layout_toStartOf="@+id/order_list_ob_index"
        android:background="@drawable/indexable"
        android:gravity="center"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:visibility="invisible" />

</RelativeLayout>